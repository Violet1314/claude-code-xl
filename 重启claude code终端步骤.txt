Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -e ".[dev]"
python -m claude_code

----------------------------------------------

更新提示词：

/plan 重构进度总结.md和README.md 帮我更新一下，规则如下：
1、重构进度总结.md  这个只更新需要更新的那部分即可，只保留最新的1个历史版本,其他历史版本移除，版本号和日期参考当前的递增；
2、README.md 这个只更新需要更新的那部分即可，只保留最新的3个历史版本,其他历史版本移除，版本号和日期参考当前最新的递增；


/plan 我只接受下面的优化建议：

文件缓存摘要质量可以更高  建议：在 Python AST 解析时提取函数签名（ast.arguments），JSON 提取值的类型信息
上下文优化缺乏自适应调参  建议：根据上下文使用率动态调整——接近限制时逐步缩小窗口，远离限制时放宽
计划模式缺乏子任务/依赖关系  建议：可以考虑简单的 depends_on: List[str] 字段，或者至少在 TodoCreate 时允许指定任务顺序约束
工具调用没有并行执行能力   建议：对于只读工具（Read、Grep、Glob、ProjectContext），可以用 concurrent.futures.ThreadPoolExecutor 并行执行。写操作仍需顺序执行以保证一致性
缺少对话摘要生成能力  建议：可以在上下文使用率 >80% 时，调用模型对中间轮次生成一条摘要消息替换原始消息。这是一个成本决策——多花一次 API 调用，但可能节省后续多轮的大量 token
TodoList 使用模块级全局变量   建议：将 TodoList 注册到 ToolContext 中，与其他全局单例统一管理
缺少 token 预算管理  建议：在 system prompt 或每轮对话中注入当前 token 使用率，让模型自主决定是否需要精简回复

修改原则：按需跑下测试，高质量完成，确保0回归，不能引入新的问题