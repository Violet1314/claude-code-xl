# Claude Code Terminal v2.8.20 项目总结

## 一、项目概述

| 项目信息 | 详情 |
|----------|------|
| 项目名称 | Claude Code Terminal |
| 当前版本 | 2.8.20 |
| 作者 | XieLong |
| 项目路径 | `E:\12-claude-code-xl\Claude-Code-CLI-main` |
| GitHub | `https://github.com/Violet1314/Claude-Code-CLI.git`（私有） |
| Python 版本 | >= 3.10 |
| 测试状态 | 178 passed |

---

## 二、技术栈

| 组件 | 技术 | 说明 |
|------|------|------|
| HTTP 客户端 | httpx | 流式请求、静默重试（5次）、错误建议 |
| 终端 UI | Rich | Panel、Markdown、Syntax、Progress |
| 输入交互 | prompt-toolkit | 多行输入、自动补全、交互式菜单 |
| 包管理 | pyproject.toml | hatchling 构建 |
| 工具调用 | Native Tool Calling | OpenAI 格式兼容 |
| Token 统计 | API 真实值 + 累加 | 流式返回 usage 累计显示（`stream_options`） |

---

## 三、目录结构

```text
claude-code/
├── pyproject.toml
├── src/claude_code/
│   ├── __init__.py
│   ├── __main__.py              # 入口点（Windows 编码修复）
│   ├── app.py                   # 主应用类（1004行）
│   ├── config/
│   │   ├── defaults.py          # 常量配置（超时、版本、计划等）
│   │   └ settings.py            # 模型/风格配置加载
│   ├── core/
│   │   ├── client.py            # APIClient（流式+错误建议）
│   │   ├── conversation.py      # Conversation（需求锚定+滑动窗口+摘要压缩）
│   │   ├── files.py             # FileManager（挂载管理）
│   │   ├── path_manager.py      # PathManager（统一路径管理+安全边界）
│   │   ├── todo.py              # TodoList（计划模式数据模型）
│   │   └ stats.py               # StatsManager（Token/费用）
│   ├── tools/
│   │   ├── base.py              # Tool基类 + PermissionLevel(3级)
│   │   ├── executor.py          # ToolExecutor（中断支持）
│   │   ├── permission.py        # PermissionManager
│   │   ├── permission_ui.py     # 权限确认 UI（中文选项）
│   │   ├── file_cache.py        # FileCacheManager
│   │   ├── syntax_checker.py    # 语法检查（10种文件）
│   │   ├── tool_calling.py      # Native Tool Calling
│   │   └ builtins/              # 10个内置工具（含Todo三件套）
│   │       ├── read.py
│   │       ├── write.py
│   │       ├── edit.py
│   │       ├── bash.py
│   │       ├── grep.py
│   │       ├── glob.py
│   │       └ ask_user.py
│   ├── commands/
│   │   ├── base.py              # Command 基类
│   │   ├── registry.py          # 命令注册表
│   │   └ handlers.py            # 8个命令（扩充帮助）
│   ├── ui/
│   │   ├── theme.py             # COLORS + ICONS
│   │   ├── console.py           # Rich封装（success/error等）
│   │   ├── components.py        # Logo + 状态栏 + 列表
│   │   ├── renderer.py          # Markdown Panel渲染
│   │   ├── input.py             # 输入处理（行号续行）
│   │   └ progress_display.py    # Bash流式进度
│   └ utils/
│       ├── paths.py             # 路径处理（workplace隔离）
│       └ tokens.py              # Token估算
├── tests/
│   ├── test_tools.py
│   ├── test_file_cache.py
│   ├── test_permission.py
│   ├── test_tool_architecture.py
│   └ test_conversation.py
├── data/
│   ├── config/
│   │   ├── api-config.json
│   │   └ system-prompts.json
│   ├── history/
│   └ stats/
└── workplace/                   # 命令执行隔离目录
```

---

## 四、核心模块详解

### 4.1 app.py - 主应用类

**核心属性**：
- `MAX_TOOL_ROUNDS = 20`：最大工具循环轮次
- `MAX_TOOLS_PER_ROUND = 40`：每轮最大工具数
- `_last_sigint`：CTRL+C 中断时间戳

**主循环流程**：
```python
def run():
    show_status_bar()           # 状态栏（模型/Token/费用）
    user_input = prompt()
    if not user_input:
        console.info("请输入内容，或使用 /help 查看帮助")  # 空输入反馈
    if user_input.startswith('/'):
        execute_command()
        console.success("命令已执行")  # 命令执行反馈
    chat(user_input)
```

**对话流程**：
```python
def chat(user_input):
    while round_count < MAX_TOOL_ROUNDS:
        messages = get_optimized_messages()  # 倒序修剪
        response, has_tools = _handle_response()
        if consecutive_failures >= 3:        # 连续错误熔断
            break
        if has_tools:
            execute_tools()
```

**中断机制**：
- 单击 CTRL+C：标记中断，流式请求/工具执行可中断
- 双击 CTRL+C（<1秒）：直接 `sys.exit(0)`

**后台计时线程**：
```python
def _refresh_timer():
    # 每0.1秒刷新进度条时间显示
    while not stop_timer_event.is_set():
        elapsed = time.time() - start_time
        progress.update(task, description=f"... {elapsed:.1f}s")
        stop_timer_event.wait(0.1)
```

**费用计算**：
```python
def _calculate_cost(input_tokens, output_tokens):
    # 费用 = (input × input_price + output × output_price) / 1,000,000
    input_price, output_price = current_model.get_prices()
    return (input_tokens * input_price + output_tokens * output_price) / 1_000_000
```

---

### 4.2 core/client.py - API 客户端

**静默重试**：前4次静默，第5次显示错误+建议

**错误建议函数**：
```python
def _get_error_suggestion(error_type):
    suggestions = {
        "ConnectError": "检查网络连接，确认 API 地址是否正确",
        "ConnectTimeout": "网络连接超时，请检查网络稳定性",
        "ReadTimeout": "服务器响应超时，可能是请求过大",
        ...
    }

def _get_http_error_suggestion(status_code):
    suggestions = {
        401: "API Key 无效或已过期",
        402: "账户余额不足",
        404: "API 端点不存在，检查 base_url",
        ...
    }
```

**超时配置**（defaults.py）：
| 参数 | 值 | 说明 |
|------|-----|------|
| CONNECT_TIMEOUT | 30s | 连接超时 |
| READ_TIMEOUT | 180s | 读取超时（大文件） |
| WRITE_TIMEOUT | 30s | 写入超时 |
| POOL_TIMEOUT | 15s | 连接池超时 |

---

### 4.3 core/conversation.py - 会话管理

**倒序优先级修剪**：
```python
def get_optimized_messages(max_tokens):
    # 保证：system + 最后一条消息
    for msg in reversed(chat_msgs[:-1]):
        if current_tokens + msg_tokens > max_tokens:
            break
        optimized.insert(0, msg.to_dict())
```

---

### 4.4 tools/base.py - 工具基类

**PermissionLevel（3级）**：
```python
class PermissionLevel(Enum):
    ONCE = "once"       # 允许本次
    SESSION = "session" # 允许本次会话全部
    NO_ONCE = "no_once" # 仅本次拒绝
```

**ToolResult 结构**：
```python
@dataclass
class ToolResult:
    success: bool
    output: str                      # 给模型的完整内容
    error: Optional[str] = None
    metadata: Dict[str, Any] = {}
    display_output: Optional[str]    # 终端显示（Rich Markup）
```

---

### 4.5 tools/executor.py - 工具执行器

**批量执行支持中断**：
```python
def execute_batch(tool_calls, interrupt_check=None):
    for tool_call in tool_calls:
        if interrupt_check and interrupt_check():
            console.print("已中断，跳过后续工具")
            break
        result = execute_single(tool_call)
```

---

### 4.6 tools/permission_ui.py - 权限确认 UI

**中文选项**：
```python
options = [
    {"name": "✓ 允许 (本次)", "value": "once", "desc": "仅本次允许，后续需再确认"},
    {"name": "✓ 允许 (会话)", "value": "session", "desc": "本次会话所有工具、所有路径自动通过"},
    {"name": "✗ 拒绝", "value": "no_once", "desc": "仅本次拒绝"},
]
```

**全局会话放行机制**：
- 选择"允许(会话)"后，`session_globally_allowed = True`
- 后续所有工具（包括敏感操作）、所有路径都自动放行
- 新会话或 `/new` 命令清除标志

---

### 4.7 ui/input.py - 输入处理

**续行显示行号**：
```python
def _get_continuation(width, line_number, is_soft_wrap):
    return [('class:input-lead', f'… {line_number + 1}> ')]
```

**交互式菜单**：
- ↑/↓ 或 j/k：上下选择
- 1-9 数字键：快速选择
- Esc 或 q：取消

---

### 4.8 commands/handlers.py - 命令实现

**扩充的帮助信息**：
- 输入技巧：6条（Enter行为差异、Ctrl+C双击退出、菜单快捷键）
- 常用命令示例：5条
- 工具详细说明：7个工具的功能和图标
- 权限确认说明：3种模式

---

## 五、工具详解

### 5.1 Read 工具

| 属性 | 值 |
|------|-----|
| 图标 | 📖 |
| 只读 | True |
| 敏感 | False |
| 最大文件 | 1MB |
| 默认行数 | 1500 |

**终端省略显示**：≥80行显示头30+尾20

### 5.2 Write 工具

| 属性 | 值 |
|------|-----|
| 图标 | ✏️ |
| 只读 | False |
| 敏感 | True |

**集成语法检查**：写入后自动检查，显示警告

### 5.3 Edit 工具

| 属性 | 值 |
|------|-----|
| 图标 | ✎ |
| 只读 | False |
| 敏感 | True |

**精确匹配策略**：只做精确匹配，多处匹配返回错误+要求添加上下文

**Read/Edit 数据一致性**：
- Read 输出保留原始内容（含空格行、行尾空格）
- 模型从 Read 复制内容直接用于 Edit 匹配
- 换行符处理统一：末尾换行不产生额外空行

### 5.4 Bash 工具

| 属性 | 值 |
|------|-----|
| 图标 | ⚡ |
| 只读 | False |
| 最大输出 | 5000字符 |
| 最大执行时间 | 120秒 |

**前置检查**：危险命令拦截、交互式命令检测

### 5.5 Grep 工具

| 属性 | 值 |
|------|-----|
| 图标 | 🔍 |
| 只读 | True |
| 最大匹配 | 30 |

### 5.6 Glob 工具

| 属性 | 值 |
|------|-----|
| 图标 | 📁 |
| 只读 | True |
| 最大结果 | 100 |

### 5.7 AskUserQuestion 工具

| 属性 | 值 |
|------|-----|
| 图标 | ❓ |
| 只读 | True |

---

## 六、UI 系统

### 6.1 颜色体系（theme.py）

| 类型 | 颜色 | 用途 |
|------|------|------|
| primary | #D97757 | Claude 官方橙色 |
| success | #4ADE80 | 成功状态 |
| error | #F87171 | 错误状态 |
| warning | #FBBF24 | 警告状态 |
| info | #60A5FA | 提示信息 |

### 6.2 图标体系

| 工具/状态 | 图标 |
|-----------|------|
| claude | ◆ |
| user | ❯ |
| success | ✓ |
| error | ✗ |
| warning | ⚠ |
| read | 📖 |
| write | ✏️ |
| edit | ✎ |
| bash | ⚡ |
| grep | 🔍 |
| glob | 📁 |
| ask | ❓ |

---

## 七、命令系统

| 命令 | 别名 | 功能 |
|------|------|------|
| /help | h, ? | 显示帮助（扩充版） |
| /new | reset, clear | 开始新会话 |
| /model | m | 切换模型 |
| /style | persona, p | 切换风格 |
| /save | s | 保存会话 |
| /history | hist | 加载历史 |
| /tools | tool | 工具执行历史 |
| /quit | exit, q | 退出 |

---

## 八、配置系统

### 8.1 defaults.py 常量

| 类别 | 参数 | 值 |
|------|------|-----|
| 文件 | MAX_FILE_SIZE | 200KB |
| 文件 | MAX_FILE_COUNT | 50 |
| 文件 | MAX_TOTAL_CHARS | 800K |
| API | MAX_TOKENS | 32768 |
| API | MAX_RETRIES | 5 |
| 对话 | DEFAULT_CONTEXT_LIMIT | 200K |

### 8.2 settings.py 配置加载

- api-config.json：API配置（base_url、api_key、models）
- system-prompts.json：多风格提示词

---

## 九、测试覆盖

| 文件 | 测试数 | 覆盖内容 |
|------|--------|----------|
| test_tools.py | ~50 | Read/Write/Edit/Glob/Grep/Bash |
| test_file_cache.py | ~20 | 缓存版本隔离 |
| test_permission.py | ~20 | 权限流程、安全上下文 |
| test_tool_architecture.py | ~5 | 架构契约 |
| test_conversation.py | ~25 | 会话修剪 |

**总计**：100 passed

---

## 十、用户体验设计

### 10.1 交互反馈

| 场景 | 反馈 |
|------|------|
| 空输入 | "请输入内容，或使用 /help 查看帮助" |
| 命令执行 | "命令已执行" |
| 中断请求 | "已中断请求" |
| API错误 | 错误描述 + 解决建议 |

### 10.2 权限系统

| 选项 | 效果 |
|------|------|
| ✓ 允许 (本次) | 仅本次通过，后续需再确认 |
| ✓ 允许 (会话) | **全会话放行**：所有工具、所有路径自动通过 |
| ✗ 拒绝 | 仅本次拒绝 |

### 10.3 输入体验

| 功能 | 实现 |
|------|------|
| 多行续行 | `… {line_number}>` 显示行号 |
| 命令补全 | `/` 开头自动补全 |
| 快捷发送 | Esc + Enter |

---

## 十一、常用命令

```powershell
# 全量测试
python -m pytest tests/ -q

# 启动应用
python -m claude_code

# 或直接运行
python src/claude_code/__main__.py
```

---

## 十二、注意事项

### Windows PowerShell 兼容性

- `mkdir data, output`（多目录逗号分隔）
- `Get-ChildItem` 或 `ls`（不带 Unix 参数）
- 不支持 `mkdir -p`、`rm -rf`、`cp -r`

### Edit 精确匹配要求

必须从 Read 结果精确复制，包含前后 2-3 行上下文

### 交互式命令禁止

- `python -i`、`vim`、`mysql` 等会被提前拦截

---

**文档版本**：v2.8.18 | **更新日期**：2026-04-18

---

## 十三、版本更新日志

## v2.8.18 更新（2026-04-18）— 终端 UI 优雅化

**核心目标**：对标官方 Claude Code 设计风格，信息密度最大化，视觉噪音最小化。

**终端显示优化**：

| 改进 | 内容 | 文件 |
|------|------|------|
| 删除 `ℹ 继续处理...` | 无信息量的过渡语，工具间由摘要行自然分隔 | `app.py` |
| Read 输出摘要化 | 终端只显示一行摘要 `📖 Read filename  N lines · X.X KB`，文件内容仍完整传给模型 | `read.py` |
| 工具结果分组打印 | 连续同类工具紧凑排列（无空行），不同工具间空行分隔 | `executor.py` |
| 跨轮次连续追踪 | `_last_displayed_tool` 追踪上一轮工具名，跨轮次连续 Read 也紧凑排列 | `executor.py`, `app.py` |
| AI Panel 间距优化 | 有工具调用时 Panel 不打前后空行，由分组打印统一控制间距 | `renderer.py`, `app.py` |
| 去掉工具 Spinner | 工具执行后直接打印摘要行，不再显示 Spinner 动画，零延迟 | `executor.py`, `progress_display.py` |

**技术细节**：

| 改动 | 说明 | 文件 |
|------|------|------|
| `ExecutionResult.display_output` | 新增字段，携带终端显示内容（Rich Markup） | `executor.py` |
| `ExecutionResult.display_shown` | 新增字段，标记摘要已在 `execute_single` 中直接打印 | `executor.py` |
| `execute_single()` 直接打印 | 工具执行完成后立即打印 `display_output`，不再经过 Spinner | `executor.py` |
| `_display_grouped_results()` | 跳过 `display_shown=True` 的结果，避免重复打印 | `executor.py` |
| `_last_displayed_tool` | 追踪上一轮打印的工具名，AI Panel 打印后重置 | `executor.py`, `app.py` |
| `render_response(has_tools)` | 新增参数：有工具调用时不打 Panel 前后空行 | `renderer.py` |
| 删除 `ToolProgressDisplay` | 不再需要通用 Spinner 组件，只保留 `BashStreamingDisplay` | `progress_display.py` |
| 删除 `ReadProgressDisplay` | 死代码清理，Read 进度统一由 executor 处理 | `progress_display.py` |
| Read 去掉内部进度条 | 删除 `_read_file_with_progress()`，大文件不再显示进度条 | `read.py` |

**改进效果**：
- 信息密度：5个文件连续读取从占满整屏 → 5行摘要，节省 80%+ 终端空间
- 视觉整洁：连续同类工具紧凑排列，消除冗余空行和过渡语
- 零延迟：去掉 Spinner 动画，工具执行完成即刻显示摘要
- 零信息丢失：文件内容仍完整传给模型，终端摘要只是视觉优化
- 178 passed，零回归

---

### v2.8.19 — 参数验证友好化 & 批量执行降级

**目标**：解决 AI 并行调用时参数缺失导致盲目重试的问题，提升错误恢复能力。

| 改动 | 说明 | 文件 |
|------|------|------|
| 参数验证友好提示 | `_build_validation_hint()` 从 Schema 提取必填参数描述，生成纠正性提示 | `executor.py` |
| 批量执行降级 | 参数错误的工具跳过继续执行，连续 3 次参数错误才终止批次 | `executor.py` |
| Schema errorMessage | 8个工具的 JSON Schema 添加 `errorMessage` 字段，帮助模型理解参数格式 | `read.py`, `write.py`, `edit.py`, `bash.py`, `glob.py`, `grep.py`, `ask_user.py`, `todo.py` |

**改进效果**：
- 参数缺失时输出纠正性提示（含参数类型和描述），AI 不再盲目重试
- 批量执行中参数错误的工具被跳过，其余工具继续执行
- 连续 3 次参数错误才终止批次，避免无限循环
- 178 passed，零回归

---

### v2.8.20 — ToolResult/ExecutionResult 重构 + CommandSafetyChecker + ToolContext + Rich Markup 安全防护

**目标**：消除数据类字段重叠、提取安全检查为独立模块、统一单例生命周期、彻底根治 `[/]` 导致程序崩溃。

| 改动 | 说明 | 文件 |
|------|------|------|
| ExecutionResult 组合持有 ToolResult | 消除 5 个重复字段（success/output/error/interrupted/display_output），通过 `@property` 代理保持向后兼容 | `base.py`, `executor.py`, `test_permission.py` |
| CommandSafetyChecker 提取 | BashTool 的危险命令/敏感命令/交互式命令/Unix语法检查全部提取为独立模块 | `command_safety.py`（新建）, `bash.py` |
| ToolContext 统一单例管理 | 全局单例（registry/permission_manager/tool_executor/path_manager）统一注册到 ToolContext，支持测试替换和生命周期管理 | `context.py`（新建）, `app.py`, `__init__.py` |
| Rich Markup 安全防护 | 猴子补丁 `_console.print` + `safe_print()` 工具函数，MarkupError 自动回退为纯文本，彻底防止 `[/]` 崩溃 | `console.py`, `safe_markup.py`（新建）, `executor.py` |

**改进效果**：
- ExecutionResult 字段重叠从 5 个降为 0，数据流更清晰
- 安全检查逻辑可独立测试和复用，BashTool 从 ~600 行降至 ~450 行
- ToolContext 统一管理单例生命周期，退出时 `tool_context.clear()` 一键清理
- **`[/]` 崩溃问题彻底根治**：任何 Rich Markup 解析失败自动回退为纯文本，程序永不崩溃
- 178 passed，零回归

---