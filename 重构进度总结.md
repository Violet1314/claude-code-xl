# Claude Code Terminal v2.8.26 项目总结

## 一、项目概述

| 项目信息 | 详情 |
|----------|------|
| 项目名称 | Claude Code Terminal |
| 当前版本 | 2.8.26 |
| 作者 | XieLong |
| 项目路径 | `E:\12-claude-code-xl\Claude-Code-CLI-main` |
| GitHub | `https://github.com/Violet1314/Claude-Code-CLI.git`（私有） |
| Python 版本 | >= 3.10 |
| 测试状态 | 185 passed |

---

## 二、技术栈

| 组件 | 技术 | 说明 |
|------|------|------|
| HTTP 客户端 | httpx | 流式请求、静默重试（5次）、错误建议 |
| 终端 UI | Rich | Panel、Markdown、Syntax、Progress |
| 输入交互 | prompt-toolkit | 多行输入、自动补全、交互式菜单 |
| 包管理 | pyproject.toml | hatchling 构建 |
| 工具调用 | Native Tool Calling | OpenAI 格式兼容 |
| Token 统计 | API 真实值 + 累加 | 流式返回 usage 累计显示（`stream_options`） |

---

## 三、目录结构

```text
claude-code/
├── pyproject.toml
├── src/claude_code/
│   ├── __init__.py
│   ├── __main__.py              # 入口点（Windows 编码修复）
│   ├── app.py                   # 主应用类（1061行）
│   ├── config/
│   │   ├── defaults.py          # 常量配置（超时、版本、计划等）
│   │   └ settings.py            # 模型/风格配置加载
│   ├── core/
│   │   ├── client.py            # APIClient（流式+错误建议）
│   │   ├── conversation.py      # Conversation（需求锚定+滑动窗口+摘要压缩）
│   │   ├── files.py             # FileManager（挂载管理）
│   │   ├── path_manager.py      # PathManager（统一路径管理+安全边界）
│   │   ├── todo.py              # TodoList（计划模式数据模型）
│   │   └ stats.py               # StatsManager（Token/费用）
│   ├── tools/
│   │   ├── base.py              # Tool基类 + PermissionLevel(3级)
│   │   ├── executor.py          # ToolExecutor（中断支持）
│   │   ├── permission.py        # PermissionManager
│   │   ├── permission_ui.py     # 权限确认 UI（中文选项）
│   │   ├── file_cache.py        # FileCacheManager
│   │   ├── syntax_checker.py    # 语法检查（10种文件）
│   │   ├── tool_calling.py      # Native Tool Calling
│   │   ├── command_safety.py    # 命令安全检查（危险/交互式/Unix语法）
│   │   ├── context.py           # ToolContext（工具执行上下文）
│   │   └ builtins/              # 10个内置工具（含Todo三件套）
│   │       ├── read.py
│   │       ├── write.py
│   │       ├── edit.py
│   │       ├── bash.py
│   │       ├── grep.py
│   │       ├── glob.py
│   │       ├── ask_user.py
│   │       └ todo.py            # TodoCreate/TodoUpdate/TodoList
│   ├── commands/
│   │   ├── base.py              # Command 基类
│   │   ├── registry.py          # 命令注册表
│   │   └ handlers.py            # 10个命令（扩充帮助+路径管理）
│   ├── ui/
│   │   ├── theme.py             # COLORS + ICONS
│   │   ├── console.py           # Rich封装（success/error等）
│   │   ├── components.py        # Logo + 状态栏 + 列表
│   │   ├── renderer.py          # Markdown Panel渲染
│   │   ├── input.py             # 输入处理（行号续行）
│   │   ├── safe_markup.py       # Rich Markup 安全转义
│   │   └ progress_display.py    # Bash流式进度
│   └ utils/
│       ├── paths.py             # 路径处理（workplace隔离）
│       └ tokens.py              # Token估算
├── tests/
│   ├── test_tools.py
│   ├── test_file_cache.py
│   ├── test_permission.py
│   ├── test_tool_architecture.py
│   ├── test_conversation.py
│   ├── test_path_manager.py
│   └ test_todo.py
├── data/
│   ├── config/
│   │   ├── api-config.json
│   │   └ system-prompts.json
│   ├── history/
│   └ stats/
└── workplace/                   # 命令执行隔离目录
```

---

## 四、核心模块详解

### 4.1 app.py - 主应用类

**核心属性**：
- `MAX_TOOL_ROUNDS = 80`：最大工具循环轮次
- `MAX_TOOLS_PER_ROUND = 50`：每轮最大工具数
- `_last_sigint`：CTRL+C 中断时间戳

**主循环流程**：
```python
def run():
    show_status_bar()           # 状态栏（模型/Token/费用）
    user_input = prompt()
    if not user_input:
        console.info("请输入内容，或使用 /help 查看帮助")  # 空输入反馈
    if user_input.startswith('/'):
        execute_command()
        console.success("命令已执行")  # 命令执行反馈
    chat(user_input)
```

**对话流程**：
```python
def chat(user_input):
    while round_count < MAX_TOOL_ROUNDS:
        messages = get_optimized_messages()  # 倒序修剪
        response, has_tools = _handle_response()
        if consecutive_failures >= 3:        # 连续错误熔断
            break
        if has_tools:
            execute_tools()
```

**中断机制**：
- 单击 CTRL+C：标记中断，流式请求/工具执行可中断
- 双击 CTRL+C（<1秒）：直接 `sys.exit(0)`

**后台计时线程**：
```python
def _refresh_timer():
    # 每0.1秒刷新进度条时间显示
    while not stop_timer_event.is_set():
        elapsed = time.time() - start_time
        progress.update(task, description=f"... {elapsed:.1f}s")
        stop_timer_event.wait(0.1)
```

**费用计算**：
```python
def _calculate_cost(input_tokens, output_tokens):
    # 费用 = (input × input_price + output × output_price) / 1,000,000
    input_price, output_price = current_model.get_prices()
    return (input_tokens * input_price + output_tokens * output_price) / 1_000_000
```

---

### 4.2 core/client.py - API 客户端

**静默重试**：前4次静默，第5次显示错误+建议

**错误建议函数**：
```python
def _get_error_suggestion(error_type):
    suggestions = {
        "ConnectError": "检查网络连接，确认 API 地址是否正确",
        "ConnectTimeout": "网络连接超时，请检查网络稳定性",
        "ReadTimeout": "服务器响应超时，可能是请求过大",
        ...
    }

def _get_http_error_suggestion(status_code):
    suggestions = {
        401: "API Key 无效或已过期",
        402: "账户余额不足",
        404: "API 端点不存在，检查 base_url",
        ...
    }
```

**超时配置**（defaults.py）：
| 参数 | 值 | 说明 |
|------|-----|------|
| CONNECT_TIMEOUT | 30s | 连接超时 |
| READ_TIMEOUT | 180s | 读取超时（大文件） |
| WRITE_TIMEOUT | 30s | 写入超时 |
| POOL_TIMEOUT | 15s | 连接池超时 |

---

### 4.3 core/conversation.py - 会话管理

**倒序优先级修剪**：
```python
def get_optimized_messages(max_tokens):
    # 保证：system + 最后一条消息
    for msg in reversed(chat_msgs[:-1]):
        if current_tokens + msg_tokens > max_tokens:
            break
        optimized.insert(0, msg.to_dict())
```

---

### 4.4 tools/base.py - 工具基类

**PermissionLevel（3级）**：
```python
class PermissionLevel(Enum):
    ONCE = "once"       # 允许本次
    SESSION = "session" # 允许本次会话全部
    NO_ONCE = "no_once" # 仅本次拒绝
```

**ToolResult 结构**：
```python
@dataclass
class ToolResult:
    success: bool
    output: str                      # 给模型的完整内容
    error: Optional[str] = None
    metadata: Dict[str, Any] = {}
    interrupted: bool = False        # 是否因用户 CTRL+C 中断
    display_output: Optional[str]    # 终端显示（Rich Markup）
    summary: Optional[str] = None    # 简短摘要（用于卡片标题或日志）
    raw_data: Optional[Any] = None   # 原始数据（供未来 Web UI 使用）
```

---

### 4.5 tools/executor.py - 工具执行器

**批量执行支持中断**：
```python
def execute_batch(tool_calls, interrupt_check=None):
    for tool_call in tool_calls:
        if interrupt_check and interrupt_check():
            console.print("已中断，跳过后续工具")
            break
        result = execute_single(tool_call)
```

---

### 4.6 tools/permission_ui.py - 权限确认 UI

**中文选项**：
```python
options = [
    {"name": "✓ 允许 (本次)", "value": "once", "desc": "仅本次允许，后续需再确认"},
    {"name": "✓ 允许 (会话)", "value": "session", "desc": "本次会话所有同类操作自动通过"},
    {"name": "✗ 拒绝", "value": "no_once", "desc": "仅本次拒绝"},
]
```

**会话放行机制**：
- 选择"允许(会话)"后，本次会话所有同类操作自动通过
- 新会话或 `/new` 命令清除标志

---

### 4.7 ui/input.py - 输入处理

**续行显示行号**：
```python
def _get_continuation(width, line_number, is_soft_wrap):
    return [('class:input-lead', f'… {line_number + 1}> ')]
```

**交互式菜单**：
- ↑/↓ 或 j/k：上下选择
- 1-9 数字键：快速选择
- Esc 或 q：取消

---

### 4.8 commands/handlers.py - 命令实现

**扩充的帮助信息**：
- 输入技巧：6条（Enter行为差异、Ctrl+C双击退出、菜单快捷键）
- 常用命令示例：5条
- 工具详细说明：7个工具的功能和图标
- 权限确认说明：3种模式

---

## 五、工具详解

### 5.1 Read 工具

| 属性 | 值 |
|------|-----|
| 图标 | ◇ |
| 只读 | True |
| 敏感 | False |
| 最大文件 | 1MB |
| 默认行数 | 1500 |

**终端省略显示**：≥80行显示头30+尾20

### 5.2 Write 工具

| 属性 | 值 |
|------|-----|
| 图标 | ▼ |
| 只读 | False |
| 敏感 | True |

**集成语法检查**：写入后自动检查，显示警告

### 5.3 Edit 工具

| 属性 | 值 |
|------|-----|
| 图标 | ✎ |
| 只读 | False |
| 敏感 | True |

**精确匹配策略**：只做精确匹配，多处匹配返回错误+要求添加上下文

**Read/Edit 数据一致性**：
- Read 输出保留原始内容（含空格行、行尾空格）
- 模型从 Read 复制内容直接用于 Edit 匹配
- 换行符处理统一：末尾换行不产生额外空行

### 5.4 Bash 工具

| 属性 | 值 |
|------|-----|
| 图标 | ▶ |
| 只读 | False |
| 最大输出 | 5000字符 |
| 最大执行时间 | 120秒 |

**前置检查**：危险命令拦截、交互式命令检测

### 5.5 Grep 工具

| 属性 | 值 |
|------|-----|
| 图标 | ◆ |
| 只读 | True |
| 最大匹配 | 30 |

### 5.6 Glob 工具

| 属性 | 值 |
|------|-----|
| 图标 | ◎ |
| 只读 | True |
| 最大结果 | 100 |

### 5.7 AskUserQuestion 工具

| 属性 | 值 |
|------|-----|
| 图标 | ◈ |
| 只读 | True |

### 5.8 TodoCreate 工具

| 属性 | 值 |
|------|-----|
| 名称 | TodoCreate |
| 只读 | False |
| 功能 | 批量创建任务计划（清空旧计划） |
| 上限 | 20 个任务 |

### 5.9 TodoUpdate 工具

| 属性 | 值 |
|------|-----|
| 名称 | TodoUpdate |
| 只读 | False |
| 功能 | 更新任务状态（pending→in_progress→completed/failed） |

### 5.10 TodoList 工具

| 属性 | 值 |
|------|-----|
| 名称 | TodoList |
| 只读 | True |
| 功能 | 查看当前任务清单及进度 |

---

## 六、UI 系统

## 六、UI 系统

### 6.1 颜色体系（theme.py）

| 类型 | 颜色 | 用途 |
|------|------|------|
| primary | #D97757 | Claude 官方橙色（唯一强调色） |
| success | #4ADE80 | 成功状态 |
| error | #F87171 | 错误状态 |
| warning | #FBBF24 | 警告状态 |
| info | #60A5FA | 提示信息 |
| border | #4A4A4A | 统一边框色（靠状态色区分重要性） |
| text_primary | #FAFAFA | 主要文字 |
| text_secondary | #A1A1AA | 次要文字 |
| text_muted | #71717A | 暗淡文字 |

### 6.2 图标体系（纯 Unicode，等宽对齐）

| 工具/状态 | 图标 | 说明 |
|-----------|------|------|
| claude | ◆ | 品牌菱形 |
| user | ❯ | 用户输入 |
| success | ✓ | 成功 |
| error | ✗ | 错误 |
| warning | ⚠ | 警告 |
| info | ℹ | 提示 |
| read | ◇ | 开放菱形 — 阅读/打开 |
| write | ▼ | 下三角 — 写入/创建 |
| edit | ✎ | 铅笔 — 编辑/修改 |
| bash | ▶ | 右三角 — 执行/运行 |
| grep | ◆ | 实心菱形 — 搜索/聚焦 |
| glob | ◎ | 靶心 — 模式匹配 |
| ask | ◈ | 菱形点 — 询问/交互 |
| file | ○ | 通用文件 |
| folder | ▸ | 目录 |

---

## 七、命令系统

| 命令 | 别名 | 功能 |
|------|------|------|
| /help | h, ? | 显示帮助（扩充版） |
| /new | reset, clear | 开始新会话 |
| /model | m | 切换模型 |
| /style | persona, p | 切换风格 |
| /save | s | 保存会话 |
| /history | hist | 加载历史 |
| /tools | tool | 工具执行历史 |
| /plan | — | 计划模式管理（start/stop） |
| /cd | — | 切换操作根目录 |
| /pwd | — | 显示当前操作根目录 |
| /quit | exit, q | 退出 |

---

## 八、配置系统

### 8.1 defaults.py 常量

| 类别 | 参数 | 值 |
|------|------|-----|
| 文件 | MAX_FILE_SIZE | 200KB |
| 文件 | MAX_FILE_COUNT | 50 |
| 文件 | MAX_TOTAL_CHARS | 800K |
| API | MAX_TOKENS | 32768 |
| API | MAX_RETRIES | 5 |
| 对话 | DEFAULT_CONTEXT_LIMIT | 200K |

### 8.2 settings.py 配置加载

- api-config.json：API配置（base_url、api_key、models）
- system-prompts.json：多风格提示词

---

## 九、测试覆盖

| 文件 | 测试数 | 覆盖内容 |
|------|--------|----------|
| test_tools.py | ~50 | Read/Write/Edit/Glob/Grep/Bash |
| test_file_cache.py | ~20 | 缓存版本隔离 |
| test_permission.py | ~20 | 权限流程、安全上下文 |
| test_tool_architecture.py | ~5 | 架构契约 |
| test_conversation.py | ~25 | 会话修剪 |

**总计**：185 passed

---

## 十、用户体验设计

### 10.1 交互反馈

| 场景 | 反馈 |
|------|------|
| 空输入 | "请输入内容，或使用 /help 查看帮助" |
| 命令执行 | "命令已执行" |
| 中断请求 | "已中断请求" |
| API错误 | 错误描述 + 解决建议 |

### 10.2 权限系统

| 选项 | 效果 |
|------|------|
| ✓ 允许 (本次) | 仅本次通过，后续需再确认 |
| ✓ 允许 (会话) | 本次会话所有同类操作自动通过 |
| ✗ 拒绝 | 仅本次拒绝 |

### 10.3 输入体验

| 功能 | 实现 |
|------|------|
| 多行续行 | `… {line_number}>` 显示行号 |
| 命令补全 | `/` 开头自动补全 |
| 快捷发送 | Esc + Enter |

---

## 十一、常用命令

```powershell
# 全量测试
python -m pytest tests/ -q

# 启动应用
python -m claude_code

# 或直接运行
python src/claude_code/__main__.py
```

---

## 十二、注意事项

### Windows PowerShell 兼容性

- `mkdir data, output`（多目录逗号分隔）
- `Get-ChildItem` 或 `ls`（不带 Unix 参数）
- 不支持 `mkdir -p`、`rm -rf`、`cp -r`

### Edit 精确匹配要求

必须从 Read 结果精确复制，包含前后 2-3 行上下文

### 交互式命令禁止

- `python -i`、`vim`、`mysql` 等会被提前拦截

---

**文档版本**：v2.8.26 | **更新日期**：2026-04-25

---

## 十三、版本更新日志

### v2.8.26 — UI 美学重构：图标纯净化 + 色彩收敛 + Panel 克制化 + 排版韵律 + 流式体验

**目标**：全面提升 UI 艺术品质——简约优雅，克制即力量。

| 改动 | 说明 | 文件 |
|------|------|------|
| 图标纯净化 | 全部 Emoji（📖✏️⚡🔍📁❓🐍📜📘📋📝📄⚙🌐🎨📎🔗🔒⭐⏱💡🔧❌✅⏭️💬📂）→ Unicode 几何符号（◇▼✎▶◆◎◈○▸→■★◷），等宽对齐，终端宽度不再错位 | `theme.py`、`permission_ui.py`、`handlers.py`、`conversation.py`、`files.py`、`base.py`、`ask_user.py`、`glob.py`、`grep.py`、`read.py`、`write.py`、`progress_display.py`、`paths.py`、`app.py`、`console.py`、`edit.py`、`todo.py` |
| 色彩收敛 | 6种灰色+新旧两套命名 → 三层灰(`text_primary/secondary/muted`) + 统一边框(`#4A4A4A`) + 品牌色 + 状态色；删除 `system/bg_dark/user/assistant/border/border_dim/border_subtle/border_default/border_emphasis/primary_dim` 共10个旧命名 | `theme.py`、`app.py`、`permission_ui.py`、`components.py`、`console.py`、`renderer.py` |
| Panel 克制化 | Bash 工具输出从 Panel 卡片 → 缩进+图标前缀（2空格缩进），工具是过程不该抢 AI 回复的视觉权重 | `progress_display.py` |
| 排版韵律 | 状态栏从一行塞满 → 分层显示（模型名突出，次要信息 dim）；所有工具输出统一缩进层级（标题 Level 0，内容 Level 1 = 2空格）；移除冗余分隔线 | `components.py`、`glob.py`、`grep.py`、`write.py`、`edit.py` |
| 流式体验 | 思考状态从 `模型名 正在思考... 3.2s 120 tok` → `⠋ thinking... 3.2s 120 tok`，等待时更安静 | `app.py` |

**改进效果**：
- **视觉统一**：图标从 Emoji 切换为 Unicode 几何符号，等宽对齐，终端宽度不再错位
- **色彩清晰**：三层灰+统一边框+品牌色+状态色，每个颜色都有不可替代的语义
- **信息层级**：Panel 只给核心内容（AI响应、权限确认），工具输出用缩进建立层级
- **呼吸感**：状态栏分层、统一缩进、移除冗余分隔线
- **安静等待**：思考状态更简洁，完成后一次性呈现完整信息
- 185 passed，零回归

### v2.8.25 — 计划模式 UI 美学重构 + `/plan stop` 命令

**目标**：提升计划模式视觉品质至艺术级——简约优雅，统一图标语言，增强仪式感。

| 改动 | 说明 | 文件 |
|------|------|------|
| 图标语言统一 | Emoji（✅🔄⏳❌）→ Unicode 符号（✓●○✗），与主题系统一致 | `core/todo.py`、`components.py`、`tools/builtins/todo.py` |
| 删除冗余标记 | 移除"← 进行中"文字，图标+颜色已足够表达 | `components.py` |
| 极简进度条 | `████████░░░░░░░` 品牌色填充 + 暗色空白，一目了然 | `components.py` |
| 任务 ID 对齐 | 数字右对齐（` 1` → `10`），t9→t10 不再错位 | `components.py` |
| 计划完成仪式感 | Rule 分隔线 + 绿色总结面板，替代一行文字 | `app.py`、`components.py` |
| `/plan` 入口视觉框架 | Panel 包裹任务描述，风格统一 | `commands/handlers.py` |
| 统计行精简 | `完成:3 \| 失败:0 \| 待处理:4` → `✓3  ✗0  ○4` | `components.py`、`tools/builtins/todo.py` |
| `/plan stop` 命令 | 主动退出计划模式，不清空会话 | `commands/handlers.py` |

**改进效果**：
- **视觉统一**：图标从 Emoji 切换为 Unicode，与全局主题系统（✓ ✗ ⚠）一致，终端宽度不再错位
- **信息密度**：进度条+图标化统计，一行传达核心信息
- **仪式感**：计划完成从一行文字升级为 Rule + 绿色面板
- **可控性**：`/plan stop` 让用户中途退出不再依赖 `/clear`
- 185 passed，零回归

---