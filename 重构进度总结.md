```markdown
# Claude Code Terminal v2.7.24 重构总结

## 一、项目概述
- **项目名称**：Claude Code Terminal
- **版本**：2.7.24（需在 `config/defaults.py` 中同步更新 `VERSION = "2.7.24"`）
- **目标**：仿照官方 Claude Code 风格，构建一个高质量的 CLI AI 编程助手，支持 AI 驱动的文件操作和命令执行。
- **项目路径**：`G:\7-Claude-code-cli\Claude-Code-CLI-main`
- **GitHub 仓库**：`https://github.com/Violet1314/Claude-Code-CLI.git`（私有）
- **最近更新**：2026-04-04 v2.7.24 交互体验优化、Bash 安全隔离与防死循环机制完成

## 二、技术栈
| 组件 | 技术 |
| --- | --- |
| Python 版本 | >= 3.10 |
| HTTP 客户端 | httpx（流式请求、静默重试机制） |
| 终端 UI | Rich (Panel, Markdown, Syntax) |
| 输入交互 | prompt-toolkit |
| 包管理 | pyproject.toml (hatchling) |
| 测试框架 | pytest |
| 工具调用 | Native Tool Calling（OpenAI 格式） |

## 三、目录结构
```text
claude-code/
├── pyproject.toml              # 包配置
├── README.md
├── 重构进度总结.md             # 本文档
├── .gitignore
│
├── src/claude_code/
│   ├── __init__.py             # 版本信息导出
│   ├── __main__.py             # 入口点 (python -m claude_code)
│   ├── app.py                  # 主应用类（新增连续错误熔断逻辑）
│   │
│   ├── config/
│   │   ├── __init__.py
│   │   ├── defaults.py         # 常量配置（VERSION="2.7.24"，WORKPLACE_DIR）
│   │   └── settings.py         # 配置加载器
│   │
│   ├── core/
│   │   ├── __init__.py
│   │   ├── client.py           # API 客户端（静默重试、异常兜底）
│   │   ├── conversation.py     # 会话管理（上下文优化）
│   │   ├── files.py            # 文件挂载管理
│   │   └── stats.py            # Token 统计与持久化（含费用累计）
│   │
│   ├── ui/                     # v2.7.23/v2.7.24 重点重构模块
│   │   ├── __init__.py         # console 导出
│   │   ├── console.py          # Rich 封装（统一消息框 Panel 化）
│   │   ├── theme.py            # 颜色、图标、Prompt 样式配置
│   │   ├── components.py       # UI 组件（Logo、状态栏、列表 Panel 化）
│   │   ├── input.py            # 输入处理、交互式菜单（样式映射修复）
│   │   ├── renderer.py         # 响应渲染（AI 回复 Panel 化）
│   │   └── progress_display.py # 工具进度显示（Bash/Edit 卡片化，补全超时方法）
│   │
│   ├── commands/
│   │   ├── __init__.py
│   │   ├── base.py             # 命令基类
│   │   ├── registry.py         # 命令注册表
│   │   └── handlers.py         # 内置命令实现
│   │
│   ├── tools/                  # 工具系统（v2.7.24 安全加固）
│   │   ├── __init__.py         # 模块导出
│   │   ├── base.py             # Tool 基类、ToolCall、ToolRegistry、PermissionLevel
│   │   ├── executor.py         # 工具执行器（路径统一解析、重复检测）
│   │   ├── permission.py       # 权限管理器（ONCE/NO_ONCE 状态机，无 ALL 逻辑）
│   │   ├── permission_ui.py    # 权限确认 UI（极简卡片，移除冗余详情）
│   │   ├── tool_calling.py     # Native Tool Calling 管理器
│   │   ├── file_cache.py       # 文件缓存管理器（读取追踪、版本管理 v0→v1→v2）
│   │   └── builtins/           # 内置工具实现
│   │       ├── __init__.py
│   │       ├── read.py         # Read 工具（卡片输出，终端压缩）
│   │       ├── write.py        # Write 工具（缓存集成 + 语法检查）
│   │       ├── edit.py         # Edit 工具（Diff 显示，版本递增，缓存更新）
│   │       ├── glob.py         # Glob 工具（卡片输出，排除目录）
│   │       ├── grep.py         # Grep 工具（卡片输出，正则支持）
│   │       ├── bash.py         # Bash 工具（路径隔离、Unix 命令自动转换、流式卡片）
│   │       └── ask_user.py     # AskUserQuestion 工具
│   │
│   └── utils/
│       ├── __init__.py
│       ├── tokens.py            # Token 估算
│       └── paths.py            # 路径工具（resolve_path / resolve_workplace_path）
│
├── workplace/                  # 隔离目录（启动时自动创建，Bash 默认 cwd）
│
├── tests/                       # 测试文件
│   ├── __init__.py
│   ├── test_tools.py            # 工具测试
│   ├── test_permission.py      # 权限系统测试
│   ├── test_conversation.py    # 会话管理测试
│   ├── test_file_cache.py      # 文件缓存测试
│   └── test_connection_recovery.py  # 连接恢复测试
│
└── data/
    ├── config/
    │   ├── api-config.json     # API 配置（含密钥）
    │   └── system-prompts.json # AI 人格提示词（强化 PowerShell 环境认知）
    ├── history/                  # 会话历史
    └── stats/                  # 统计数据
```

## 四、命令列表
当前可用命令（来自 `commands/handlers.py`）：
| 命令 | 别名 | 说明 |
| --- | --- | --- |
| /help | /h, /? | 显示命令帮助 |
| /new | /reset, /clear | 开始新会话 |
| /model | /m | 切换 AI 模型 |
| /style | /persona, /p | 切换 AI 风格 |
| /save | /s | 保存当前会话 |
| /history | /hist | 查看历史会话 |
| /tools | /tool | 查看工具执行历史 |
| /quit | /q, /exit | 退出程序 |

### 四-A、权限菜单
- **只读工具**（Read、Glob、Grep）：自动放行，无需确认。
- **写入/执行工具**（Write、Edit、Bash）：
  | 选择 | 效果 |
  | --- | --- |
  | Yes (once) | 允许操作（同工具同路径会话内缓存，后续自动通过） |
  | No (once) | 仅本次拒绝 |
  | Esc/q | 取消并中断后续执行 |
- **敏感操作例外**：`rm`、`Remove-Item`、`sudo`、`git push` 等敏感操作不缓存权限，每次都需确认。
- **注意**：`ALL`（全局授权）选项已彻底移除，权限级别仅保留 `ONCE` 和 `NO_ONCE`。

## 五、工具系统

### 5.1 工具调用模式
统一使用 Native Tool Calling（v2.7.0 起）。通过 API 原生支持实现工具调用，使用 OpenAI 格式的 `tools` 参数。支持的模型：GPT、Claude、DeepSeek、Qwen、Gemini、GLM 等主流模型。

### 5.2 文件缓存系统
核心设计（v2.7.22 代码级彻底落地）：
- **首次读取**：读取文件 → 完整缓存 → 返回内容 + 引用。≤1500 行直接显示完整内容；>1500 行显示摘要 + 结构概览。
- **后续操作**：AI 基于缓存回答 → 无需重新读取。写入/编辑后自动更新版本（v0 → v1 → v2...）。
- **重复读取检测**：第 1 次正常读取；第 2-4 次终端显示黄色警告；第 5 次起拦截，不执行读取。

### 5.3 内置工具
| 工具 | 参数 | 说明 |
| --- | --- | --- |
| Read | file_path, offset?, limit? | 读取文件（卡片式输出，终端压缩显示，缓存引用） |
| Write | file_path, content | 创建或覆盖文件（集成缓存 + 语法检查） |
| Edit | file_path, old_string, new_string | 精确替换（含 diff 显示，多处匹配警告，缓存版本递增） |
| Glob | pattern, path? | 按文件名模式搜索（卡片式输出，文件类型统计） |
| Grep | pattern, path?, type? | 按内容搜索（卡片式输出，正则支持） |
| Bash | command, timeout?, cwd? | 执行 shell 命令（**默认隔离至 workplace/**，支持 Unix 命令自动转换） |
| AskUserQuestion | question, options? | 向用户询问选择（选项菜单 + 自由输入） |

### 5.4 多轮执行与防死循环
- **单轮限制**：最多执行 10 个工具调用。
- **最大循环**：提升至 **10 轮**（配合熔断机制）。
- **连续错误熔断**：检测到连续 3 次相同的工具执行错误时，强制中断循环并通知模型停止重试。
- **所有工具被跳过时**：立即停止循环。

## 六、已完成功能

| 功能 | 状态 | 说明 |
| --- | --- | --- |
| UI 重构 (v2.7.23) | ✅ | 全面采用 Rich Panel 卡片体系，移除手动边框，统一视觉风格 |
| 圆角卡片渲染 | ✅ | AI 回复、工具结果、权限菜单均使用 ROUNDED Box |
| 语义化配色 | ✅ | 统一 COLORS 字典，品牌色 #D97757，状态色柔和化 |
| 极简提示符 | ✅ | 修复 Prompt Toolkit 样式映射，显示橙色 ❯ |
| 多模型切换 | ✅ | 交互式菜单 |
| 多风格切换 | ✅ | 5 种 AI 人格 |
| 流式输出 | ✅ | SSE 流式响应 |
| 上下文优化 | ✅ | 倒序优先级修剪 |
| 会话保存/加载 | ✅ | JSON 格式 |
| Token 统计 | ✅ | 会话统计 + 持久化 |
| Powerline 状态栏 | ✅ | 显示模型/价格/Token/费用 |
| Read/Write/Edit/Glob/Grep/Bash/AskUserQuestion | ✅ | 7 个内置工具（v2.7.24 契约已统一） |
| 权限确认系统 | ✅ | once 模式，敏感操作不缓存 |
| 多轮执行与熔断 | ✅ | 最大 10 轮，连续 3 次同质错误自动熔断 |
| 文件缓存系统 | ✅ | 统一管理：版本追踪 + 读取计数 + 行范围记录 |
| Native Tool Calling | ✅ | v2.7.0 统一使用原生模式 |
| 卡片式输出 | ✅ | Read/Grep/Glob/Bash 美化 |
| 编程名言 | ✅ | 欢迎界面随机显示 |
| 文件类型图标 | ✅ | 根据扩展名显示图标 |
| 终端显示压缩 | ✅ | 超过 80 行只显示首尾 |
| Workplace 隔离目录 | ✅ | **Bash/Write/Edit 默认隔离至 workplace/** |
| 重复读取检测 | ✅ | 第 2-4 次警告，第 5 次拦截（路径已统一解析） |
| PowerShell 敏感命令检测 | ✅ | Remove-Item 等 PowerShell 命令 |
| 危险命令提前拦截 | ✅ | 权限确认前直接拦截危险命令 |
| Write 集成缓存 | ✅ | Write 写入后更新 file_cache |
| Bash 失败显示错误 | ✅ | Bash 流式卡片统一显示，不重复 |
| 权限 ALL 清理 | ✅ | 移除未使用的全局授权逻辑 |
| 只读工具自动放行 | ✅ | Read/Glob/Grep 跳过权限确认弹窗 |
| Read/Glob/Grep 不隔离 | ✅ | 使用 resolve_path，相对路径基于项目根目录 |
| Glob/Grep 排除无关目录 | ✅ | 自动排除 .venv、node_modules、__pycache__ 等 |
| 代码级空格/断字清理 | ✅ | v2.7.22 核心：全模块字典键、变量名、正则尾部空格清零 |
| 全链路交互验收 | ✅ | v2.7.22 核心：5 项真实场景测试 100% 通过 |
| **UI 静默重试 (v2.7.24)** | ✅ | API 连接重试不再刷屏，仅最终失败时报错 |
| **权限卡片精简 (v2.7.24)** | ✅ | 移除冗长的 Details 字段，只保留核心操作 |
| **Bash 路径隔离 (v2.7.24)** | ✅ | 默认 cwd 指向 workplace/，防止污染项目根目录 |
| **Unix 命令兼容 (v2.7.24)** | ✅ | 自动转换 ls/cat/rm 等常见 Unix 命令为 PowerShell 等效命令 |

## 七、运行方式

```powershell
# 进入项目目录
cd G:\7-Claude-code-cli\Claude-Code-CLI-main

# 激活虚拟环境
.\.venv\Scripts\Activate.ps1

# 运行测试
python -m pytest tests/ -q --ignore=tests/test_api_stability.py --ignore=tests/test_connection_recovery.py

# 运行应用
python -m claude_code
```

## 八、测试覆盖

### 测试文件统计
| 测试文件 | 测试用例 | 覆盖模块 |
| --- | --- | --- |
| test_tools.py | 19 | builtins/*.py |
| test_permission.py | 14 | tools/permission.py, executor.py |
| test_conversation.py | 30 | core/conversation.py |
| test_file_cache.py | 34 | tools/file_cache.py（含读取追踪） |
| test_connection_recovery.py | 4 | core/client.py |
| **总计** | **101** | - |

### 最近测试结果
`93 passed in 0.30s` (2026-04-04, 全量模块通过，排除网络相关测试)

## 九、给下一个 AI 的提示

### 核心架构
- **入口点**：`__main__.py` → `app.py` 的 `Application.run()`
- **版本号位置**：`config/defaults.py` 中的 `VERSION`
- **工具调用**：统一使用 Native Tool Calling

### 工具系统（v2.7.24 已稳固）
- **工具调用管理器**：`tools/tool_calling.py`，仅支持 native 模式
- **文件缓存管理器**：`tools/file_cache.py`，统一管理内容缓存 + 读取追踪 + 版本管理
- **参数验证**：`executor.py` 在执行前调用 `tool.validate_parameters()`
- **执行中断**：用户取消时立即停止后续工具执行
- **重复检测**：`executor.py` 通过 `file_cache.get_read_count()` 检测，第 2-4 次警告，第 5 次拦截（阈值 `>=4`）
- **代码质量**：全工具类已彻底清理空格断字、字典键错位、变量名断裂，API 契约严格对齐，无历史技术债。

### 路径系统（重要！v2.7.24 更新）
- **Workplace 隔离**：**Write/Edit/Bash** 的相对路径自动解析到 `workplace/` 目录。
- **Bash 特殊逻辑**：
  - 默认 `cwd` 强制指向 `workplace/`。
  - 内置 `_translate_unix_command` 方法，自动将 `ls -la` 等转换为 `Get-ChildItem -Force`。
  - 模型提示词已强化：“当你执行 Bash 时，你的脚下就是 workplace 目录”。
- **Read/Glob/Grep 不隔离**：使用 `resolve_path()`，相对路径基于当前工作目录（项目根）。
- **绝对路径**：所有工具都可访问任意位置，不受隔离限制。
- **路径统一解析**：`executor.py` 中重复读取检测使用 `resolve_path()` 统一路径。

### 安全机制
- **只读自动放行**：`executor.py` 中 `tool.is_read_only()` 为 True 时跳过权限确认。
- **敏感命令**：`bash.py` 中 `SENSITIVE_PATTERNS`，每次需确认（不缓存权限）。
- **危险命令**：`DANGEROUS_PATTERNS`，直接拦截不显示权限菜单。
- **拦截时机**：危险命令在 `executor.py` 权限确认前检查。
- **权限级别**：只有 `ONCE` 和 `NO_ONCE`。

### API 与网络
- **用户使用中转 API**：`base_url` 是 `https://yunwu.ai/v1`
- **异常兜底**：`client.py` 使用 `except httpx.HTTPError` 捕获所有 httpx 异常
- **静默重试**：前 N-1 次重试不打印任何日志，仅在最终失败时显示一次错误。
- **超时配置**：连接 30s，读取 180s，重试 5 次。

### UI 系统 (v2.7.24 优化后)
- **主题配置**：`ui/theme.py`，包含颜色、图标、Prompt Toolkit 样式。
- **卡片体系**：全面使用 Rich `Panel` (Box: ROUNDED) 替代手动边框。
- **文件图标**：`.py` 显示 `🐍`，`.js` 显示 `📜` 等。
- **边框颜色**：`border_subtle = "#6A6A6A"`，深色终端可见。
- **中文宽度处理**：`ui/input.py` 中 `display_width()` 函数，中文=2宽度。
- **Rich markup 转义**：使用 `rich.markup.escape()` 转义文件内容中的特殊字符。
- **Bash 流式卡片**：`progress_display.py` 的 `BashStreamingDisplay`，成功和失败都在卡片内显示，补全了 `is_timeout` 方法。
- **Edit Diff 渲染**：`permission_ui.py` 正确识别并渲染彩色 Diff 输出。
- **权限卡片精简**：移除了冗长的 `details` 字段，保持界面清爽。

### 费用计算
- **价格解析**：`settings.py` 中 `ModelConfig.get_prices()` 返回 `(input_price, output_price)`
- **费用公式**：`(input_tokens × input_price + output_tokens × output_price) / 1,000,000`
- **价格配置**：`price` 字段已包含分组倍率，无需额外处理

### 测试与验证
- **测试命令**：只跑相关模块，用 `-q` 安静模式
- **API 稳定性测试**：`test_api_stability.py` 平时忽略
- **v2.7.24 验收基线**：`93 passed` + UI 专项交互验证全通过 + Bash 隔离验证通过

## 十、v2.7.24 交互与安全重构详情

v2.7.23 完成 UI 视觉现代化，v2.7.24 专注于**交互噪音消除**、**Bash 安全隔离**与**智能防死循环**。

### 核心重构清单
| 序号 | 问题/模块 | 修复/重构内容 | 状态 |
| --- | --- | --- | --- |
| ① | client.py | 实现静默重试，前 4 次失败不打印日志，仅最终失败报错 | ✅ |
| ② | permission_ui.py | 精简权限卡片，移除 `details` 字段，只保留核心操作描述 | ✅ |
| ③ | bash.py | 默认 `cwd` 强制指向 `workplace/`，实现相对路径隔离 | ✅ |
| ④ | bash.py | 新增 `_translate_unix_command`，自动转换常见 Unix 命令 | ✅ |
| ⑤ | progress_display.py | 补全 `BashStreamingDisplay.is_timeout()` 等方法，修复崩溃 | ✅ |
| ⑥ | app.py | 引入连续错误追踪，连续 3 次同质错误触发熔断 | ✅ |
| ⑦ | system-prompts.json | 强化 `expert` 提示词，明确 Bash 环境为 PowerShell 且 cwd 在 workplace | ✅ |
| ⑧ | app.py | 将 `MAX_TOOL_ROUNDS` 从 5 提升至 10，配合熔断机制使用 | ✅ |

### 交互验收记录（2026-04-04）
| 验收项 | 测试场景 | 预期结果 | 实际结果 |
| --- | --- | --- | --- |
| 静默重试 | 模拟网络断开 | 前 4 次不刷屏，第 5 次显示最终错误 | ✅ 界面整洁，无冗余警告 |
| 权限卡片 | 调用 Write 工具 | 只显示 `操作: Write(...)`，无长篇详情 | ✅ 卡片清爽，焦点集中 |
| Bash 隔离 | 执行 `mkdir test` | 文件夹创建于 `workplace/test`，而非根目录 | ✅ 隔离生效，项目根目录干净 |
| Unix 转换 | 执行 `ls -la` | 自动转换为 `Get-ChildItem -Force` 并成功执行 | ✅ 模型无需学习 PS 语法即可工作 |
| 防死循环 | 故意执行错误 Bash 命令 3 次 | 第 3 次后触发熔断，模型收到停止指令 | ✅ 成功中断无效重试 |
| Bash 位置感知 | 执行 `Get-ChildItem` | 列出 `workplace/` 内容，而非尝试访问 `workplace/workplace` | ✅ 模型认知与代码逻辑对齐 |

## 十一、工具路径处理最终状态 (v2.7.24)

| 工具 | 路径解析函数 | 隔离设计 | 权限确认 |
| --- | --- | --- | --- |
| Read | resolve_path | ❌ 不隔离 | ✅ 自动放行 |
| Glob | resolve_path | ❌ 不隔离 | ✅ 自动放行 |
| Grep | resolve_path | ❌ 不隔离 | ✅ 自动放行 |
| Write | resolve_workplace_path | ✅ 隔离 | ⚠️ 需确认 |
| Edit | resolve_workplace_path | ✅ 隔离 | ⚠️ 需确认 |
| **Bash** | **强制 workplace (相对路径)** | **✅ 隔离** | **⚠️ 需确认** |
| executor.py 重复检测 | resolve_path | 与 Read 路径统一 | — |

**Glob/Grep 自动排除目录**：`.venv`、`node_modules`、`__pycache__`、`.git`、`dist`、`build`、`.idea`、`.vscode`、`vendor`、`.gradle`、`.cargo` 等。

## 十二、验收状态

### ✅ v2.7.24 全部验收通过 (2026-04-04)

| 序号 | 验收项 | 状态 | 验收证据 |
| --- | --- | --- | --- |
| ① | UI 视觉一致性 | ✅ 通过 | 所有组件均为圆角 Panel，无手动边框残留 |
| ② | 样式映射正确性 | ✅ 通过 | 提示符、菜单、Diff 颜色均正确渲染 |
| ③ | 工具输出收敛 | ✅ 通过 | Bash/Read/Grep 长输出被有效折叠或卡片化 |
| ④ | 缓存链路完整性 | ✅ 通过 | v0→v1→v2 版本递增正常 |
| ⑤ | 单元测试基线 | ✅ 通过 | 93 passed in 0.30s |
| ⑥ | 代码质量 | ✅ 通过 | UI 模块逻辑清晰，无硬编码样式 |
| ⑦ | Bash 安全隔离 | ✅ 通过 | 相对路径命令均在 `workplace/` 下执行 |
| ⑧ | 静默重试机制 | ✅ 通过 | 网络波动时界面无刷屏，体验流畅 |
| ⑨ | 防死循环机制 | ✅ 通过 | 连续同质错误能触发熔断并通知模型 |

**全量测试**：`93 passed in 0.30s`

## 十三、下一轮测试计划

### 背景
v2.7.24 在保持 UI 美观的基础上，显著提升了系统的**鲁棒性**（静默重试）、**安全性**（Bash 隔离）和**智能性**（防死循环、Unix 兼容）。

### 经验沉淀（v2.7.24）
- **静默优于噪杂**：对于自动恢复机制（如重试），用户只关心最终结果，中间过程应隐藏。
- **认知对齐**：代码层的逻辑（如 cwd 重定向）必须通过 Prompt 明确告知模型，否则模型会产生“幻觉路径”。
- **兜底策略**：自动转换（Unix to PowerShell）是提升用户体验的利器，但不能完全依赖，仍需模型具备基本的 PS 意识。

### 建议迭代方向
1. **core/client.py 重构**：进一步优化 HTTP 流式解析，支持更复杂的中转 API 断连恢复。
2. **app.py 主循环优化**：增强上下文窗口动态修剪策略，支持更大规模的代码库操作。
3. **生产级压力测试**：超大文件（>50MB）读取、深层目录 `Glob` 遍历、复杂正则 `Grep` 扫描、并发权限拦截稳定性。

### 常用测试命令
```powershell
# 全量测试
python -m pytest tests/ -q --ignore=tests/test_api_stability.py --ignore=tests/test_connection_recovery.py

# 工具相关测试
python -m pytest tests/test_tools.py tests/test_file_cache.py -q

# 权限测试
python -m pytest tests/test_permission.py -q

# 启动应用
python -m claude_code
```